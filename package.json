{
  "devDependencies": {
    "vitest": "^4.1.1",
    "testcafe": "^3.7.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "lint": "eslint . --fix",
    "lint-check": "eslint .",
    "test": "vitest",
    "test-coverage": "vitest run --coverage",
    "tsc": "tsc"
  }
}
