{
  "devDependencies": {
    "vitest": "^4.1.1"
  },
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "lint": "eslint . --fix",
    "lint-check": "eslint .",
    "test": "vitest",
    "test-coverage": "vitest run --coverage",
    "tsc": "tsc"
  }
}
